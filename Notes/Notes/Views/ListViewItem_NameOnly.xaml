<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Notes.Views.ListViewItem_NameOnly">
  <ContentView.Content>
        <Grid Margin="10,10,10,10">

            <Grid.RowDefinitions>
                <RowDefinition Height="24" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label x:Name="IconLabel"
                   Grid.Column="0"
                   Style="{StaticResource Style_ListViewItemIcon}"
                   />
            <Label x:Name="NameLabel"
                   Grid.Column="2"
                   Style="{StaticResource Style_ListViewItemName}"
                   />
            <Label x:Name="QuickAccessMarkerLabel" 
                   Grid.Column="3"
                   Style="{StaticResource Style_ListViewItemQuickAccessMarker}"/>
        </Grid>
    </ContentView.Content>
</ContentView>